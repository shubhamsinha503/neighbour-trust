# water agent — not built yet.
#
# Phase 1 is deliberately air_quality only: proving fetch -> normalize -> store
# -> serve -> render once before multiplying it by six. See docs/build-roadmap.md
# Phase 2 for what lands here and from which sources.
